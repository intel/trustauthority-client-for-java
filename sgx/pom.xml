<!-- sgx/pom.xml -->
<project>
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>trust_authority_client</groupId>
        <artifactId>trust-authority-client-java</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>sgx</artifactId>

    <build>
        <sourceDirectory>.</sourceDirectory>
        <resources>
            <resource>
                <directory>../resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version>
                <configuration>
                    <source>1.8</source>
                    <target>1.8</target>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>trust_authority_client</groupId>
            <artifactId>connector</artifactId>
            <version>1.0.0</version>
        </dependency>
    </dependencies>
</project>
